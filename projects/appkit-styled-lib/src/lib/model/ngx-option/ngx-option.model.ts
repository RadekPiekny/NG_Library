export interface INgxOptionProps {
    multi: boolean;
    selected: boolean;
    value: any;
    displayValue: string;
    innerHTML?: string; // if the dev decides to use some icon or whatever else
}